---
description: 产品专家 Agent 命令路由
globs:
alwaysApply: false
---

# 产品专家 Agent 命令路由

当用户输入以下命令时，优先读取对应 `SKILL.md` 并严格按 skill 内的流程、检查点与脚本入口执行：

## Research Toolkit 调研分析命令

- `/深度调研 [产品/方向]`：进入 `skills/research-toolkit/SKILL.md`，AI 自动识别任务类型（单产品/方向/全景/用户研究），走完整调研流程
- `/海外调研 [主题]`：进入 `skills/research-toolkit/SKILL.md` 的海外通道（`protocols/overseas-research.md`），用 last30days 引擎对 Reddit/X/YouTube/HN/TikTok/Polymarket 等海外平台采集并归一，引擎不可用时按协议降级
- `/爬取 [平台] [关键词]`：作为 `research-toolkit` 的主动社媒采集子流程执行（MediaCrawler / XHS-Downloader）
- `/体验引导`：作为 `research-toolkit` 的 App/Web 实测引导子流程执行
- `/调研安装`：安装 DeerFlow + MediaCrawler + XHS-Downloader 工具链
- `/调研体检`：环境 + MCP + API + 工具链全面就绪检查
- `/调研授权`：小红书 QR 二维码授权流程
- `/调研打包`：构建可分发的 Skill zip 包

### 自然语言触发

以下表达也会路由到 `research-toolkit`：
- "帮我调研下 XXX" / "看看 XXX 怎么样" / "分析一下 XXX"
- "XXX 竞品分析" / "XXX 产品调研" / "XXX 市场调研"
- "做个用户研究" / "看看用户怎么说" / "挖一下用户痛点"
- "这个赛道怎么样" / "方向可行性分析"
- "海外怎么看 XXX" / "Reddit/X/YouTube 上 XXX 口碑" / "XXX 出海/海外竞品舆情" → 海外通道（`/海外调研`）

## AIBI SQL 数据查询命令

- `/SQL [自然语言提问]`：进入 `skills/aibi-query/SKILL.md`，**快速版** — 聊天内直接输出结论+表格+图表，不写任何文件
- `/SQL深度 [自然语言提问]`：进入 `skills/aibi-query/SKILL.md`，**深度版** — 生成 HTML 看板 + CSV 数据表 + 完整洞察

### 自然语言触发

以下表达也会路由到 `aibi-query`（默认快速版，含"报告/看板/HTML/导出"等词自动切深度版）：
- "帮我查下数据" / "跑个数" / "看下指标"
- "星卡数据" / "绘本用户情况" / "泡泡玩具订阅"
- "运营分析" / "数据看板" / "出个数据报告"

### 注意事项

- 仅 sparklab_starcard、sparklab_picbook、sg_sparklab_poptoy 三个库有线上数据
- 其他库（测肤/社交/作业等）暂无部署，匹配到时需告知用户
- 首次使用需引导用户申请 DBOPS 只读权限并提供 SSO Token

## 其他 Skill 命令

- `/产品策划`：进入 `skills/education-prd-orchestrator/SKILL.md`
- `/产品策划校验`：进入 `skills/education-prd-orchestrator/SKILL.md`，以校验已有产物为主
- `/AI策划`：进入 `skills/ai-planning-orchestrator/SKILL.md`，默认串起 AI PRD、AI脚本、AI测试
- `/AIPRD`：只执行 AI PRD 子流程
- `/AI脚本`：只执行脚本与样例子流程
- `/AI测试`：只执行测试与测试报告子流程
- `/Demo开发`：进入 `skills/product-demo-orchestrator/SKILL.md`
- `/Demo设计系统` / `/Demo脚手架` / `/Demo打磨` / `/Demo校验`：Demo 子流程
- `/Demo上线`：将已完成的 Web/H5 demo 部署到云端（Vercel/Surge/Netlify/Cloudflare），进入 `skills/product-demo-orchestrator/SKILL.md` 的部署上线流程
- `/查看能力`：列出核心能力点与对应子命令
- `/检查Agent更新`：查看当前版本与新增能力
- `/更新请求` / `/经验写回`：整理可沉淀的方法并写回 Agent 仓库
- `/安全扫描`：按 `policies/agent-security-scan.md` 用 AgentShield 扫描 Agent 配置/指令文件的安全漏洞（密钥/权限/注入/MCP/Hook），不可用时降级为人工自检清单；写回前应先跑一次
- `/图片提示词 [需求]`：按 `policies/image-prompt-connector.md` 用 nano-banana-pro 从 10000+ Gemini 图片提示词里检索/改写出图提示词（产品配图/Demo 视觉/海报/缩略图），按需 `npx skills i` 安装、不可用降级为手写提示词骨架

## 执行原则

1. 先读对应 skill，再行动。不要凭命令名猜测流程。
2. 调研分析默认是完整闭环：任务卡 → 采集 → 分析 → 体验 → 写作 → 核查 → 导出。不跳步。
3. 遇到 skill 内要求的 user checkpoint，必须停下来确认，不要编造。
4. 接手人首次使用时，按 SKILL.md 中的"傻瓜式三件套"引导协议执行。
