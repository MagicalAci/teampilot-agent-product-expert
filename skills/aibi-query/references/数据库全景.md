# 星火工坊 MySQL 数据库全景

> 生成时间：2026-04-02 | 环境：PRO | 单元：HZUNIT

---

## 一、数据库总览

| # | 数据库 | PRO ID | 表数量 | 状态 | 说明 |
|---|--------|--------|--------|------|------|
| 1 | **sparklab_starcard** | 1198 | 26 | ✅ 可查 | 星卡（追星小卡鉴定 + 站姐活动电商） |
| 2 | **sparklab_picbook** | 1217 | 19 | ✅ 可查 | 绘本（AI 绘本阅读硬件 + 语音合成） |
| 3 | **sg_sparklab_poptoy** | 2078 | 18 | ✅ 可查 | 泡泡玩具（AI 玩偶社区 + 聊天 + IAP 订阅） |
| 4 | **sparklab_homework_monito** | 1258 | 11 | ✅ 可查 | 作业监督（AI 专注力监测 + 学习报告） |
| 5 | sparklab_skinscan | 1215 | 0 | ⚠️ 无物理源 | PRO 有逻辑库但无物理实例 |
| 6 | sparklab_tellme | 1260 | 0 | ⚠️ 无物理源 | PRO 有逻辑库但无物理实例 |
| 7 | sparklab_aidating | 2929 | ? | ⚠️ 需申请权限 | PRO 有物理源，需单独申请只读权限 |
| 8 | calio_mvp | 2932 | ? | ⚠️ 需申请权限 | PRO 有物理源，需单独申请只读权限 |

---

## 二、sparklab_starcard（星卡）— 26 张表

### 业务模块划分

| 模块 | 表名 | 说明 |
|------|------|------|
| **用户** | t_user, t_starcard_user | 用户基础信息、微信绑定 |
| **明星** | t_star_info, t_starcard_star_info | 明星团体管理（内娱/韩娱/泰娱/欧美） |
| **用户-明星关系** | t_starcard_user_star_relation | 关注、加入粉丝站 |
| **明星公告** | t_starcard_star_notice | 明星页公告内容 |
| **活动** | t_activity, t_starcard_activity | 站姐发起的商品活动 |
| **活动扩展** | t_activity_media, t_starcard_activity_media | 活动图片/视频 |
| | t_activity_participant, t_starcard_activity_participant | 活动参与者 |
| | t_starcard_activity_comment | 活动评论 |
| | t_starcard_activity_extend | 活动扩展信息 |
| | t_starcard_activity_sku | 活动 SKU |
| | t_starcard_activity_spec, t_starcard_activity_spec_value | 活动规格 |
| **订单** | t_order, t_starcard_order | 购买订单（含物流、支付、退款） |
| **AI 鉴定** | t_identify_task, t_starcard_identify_task | 小卡鉴定任务（正面/背面/纹路/像素） |
| | t_identify_template, t_starcard_identify_template | 鉴定模板（含 AI Prompt） |
| **统计** | t_index_stat, t_starcard_index_stat | 累计鉴定数量 |
| **营销** | t_starcard_marketing_position | 开屏/弹窗营销位 |

### 关键字段速查

**t_starcard_user** — 用户表
- `guid` (PK), `user_new_id` (对外ID), `wechat_openid`, `nickname`, `avatar`, `permission` (1=站姐, 0=普通)

**t_starcard_activity** — 活动表
- `guid` (PK), `user_id` (发起人), `activity_title`, `product_amount`, `stock_quantity`, `purchase_quantity`, `activity_status` (0待审核/1已发布/2未通过/3已结束/4已关闭)

**t_starcard_order** — 订单表
- `guid` (PK), `order_no`, `user_id` (买家), `seller_user_id` (卖家), `activity_id`, `pay_amount`, `order_status`

**t_starcard_identify_task** — AI 鉴定任务
- `guid` (PK), `user_id`, `template_id`, `ai_result` (0不通过/1通过), `ai_reason`, `ai_front_result`, `ai_back_result`, `ai_frontTexture_result`, `ai_backLogo_result`, `ai_pixel_result`

**t_starcard_star_info** — 明星信息
- `guid` (PK), `star_name`, `category` (0内娱/1韩娱/2泰娱/3欧美)

**t_starcard_user_star_relation** — 用户-明星关系
- `user_id`, `star_id`, `is_joined`, `is_followed`

---

## 三、sparklab_picbook（绘本）— 19 张表

### 业务模块划分

| 模块 | 表名 | 说明 |
|------|------|------|
| **用户** | t_pic_book_user_info | 用户基础信息 |
| **绘本** | t_pic_book_meta | 绘本元数据 |
| | t_pic_book_page_detail | 绘本页面详情 |
| | t_pic_book_user_shelf | 用户书架（支持 ISBN 扫描/用户上传） |
| **设备** | t_pic_book_device_user_binding | 设备-用户绑定关系 |
| | t_pic_book_device_event | 设备事件上报（属性/事件） |
| | t_pic_book_sticker_config | 贴纸配置 |
| **阅读** | t_pic_book_reading_record | 阅读记录（时长、进度） |
| | t_pic_book_reading_daily_stat | 每日阅读统计 |
| | t_pic_book_reading_mode | 阅读模式（精简/渲染/互动） |
| **对话** | t_pic_book_conversation_record | AI 对话记录 |
| **语音** | t_pic_book_voiceprint | 声纹管理（家长/系统） |
| **AI 报告** | t_pic_book_ai_report | AI 阅读报告（兴趣点 + 推荐建议） |
| **任务** | t_pic_book_task | 异步任务（图片识别/语音合成） |
| **反馈** | t_pic_book_feedback | 用户反馈（功能/内容/音频/其他） |
| **纠错** | t_pic_book_correction | 绘本内容纠错 |
| **配置** | t_pic_book_guide_image | 引导图 + 功能开关 |
| **日志** | t_pic_book_api_log | API 调用日志 |
| **测试** | t_test_sticker_audio | 贴纸音频测试 |

### 关键字段速查

**t_pic_book_user_info** — 用户
- `guid` (PK), `nickname`, `user_new_id`

**t_pic_book_device_user_binding** — 设备绑定
- `device_id`, `user_id`, `status` (1=绑定, 0=解绑)

**t_pic_book_reading_record** — 阅读记录
- `guid` (PK), `user_id`, `device_id`, `book_id`, `page_index`, `start_time`, `end_time`, `duration` (秒), `progress_pct`

**t_pic_book_reading_daily_stat** — 每日统计
- `guid` (PK), `user_id`, `stat_date`, `total_duration` (秒), `total_minutes`, `book_count`, `record_count`

**t_pic_book_conversation_record** — AI 对话
- `guid` (PK), `user_id`, `device_id`, `conversation_type`, `round_id`, `sequence_num`, `conversation_context`

**t_pic_book_voiceprint** — 声纹
- `guid` (PK), `user_id`, `voiceprint_type` (PARENT/SYSTEM), `generate_status` (0=处理中/1=成功/2=失败)

**t_pic_book_user_shelf** — 书架
- `user_id`, `book_id`, `book_name`, `book_source` (0=ISBN扫码/1=用户上传)

**t_pic_book_feedback** — 反馈
- `feedback_type` (0功能/1内容/2音频/3其他), `status` (0未处理/1处理中/2已处理/3已关闭)

**t_pic_book_reading_mode** — 阅读模式
- `mode_code` (CONCISE/RENDER/INTERACTIVE), `mode_name`, `is_default`, `is_active`

---

## 四、sg_sparklab_poptoy（泡泡玩具）— 18 张表

### 业务模块划分

| 模块 | 表名 | 说明 |
|------|------|------|
| **用户** | t_user_info, t_account | 用户信息 + 账户（能量、订阅） |
| **社区** | t_community | 社区管理（默认/用户社区） |
| **房间** | t_room | 房间配置 |
| **住户/玩偶** | t_room_resident | AI 玩偶（人设、声音、性格、场景） |
| **聊天** | t_chat_session | 聊天会话 |
| | t_chat_message | 聊天消息（文字/语音） |
| | t_chat_summary | 对话总结（日/周） |
| **内容** | t_article | 文章/内容管理 |
| **素材** | t_material | 素材库 |
| | t_media | 媒体资源 |
| | t_label_relation | 标签关联 |
| **AI 提示词** | t_prompt | AI Prompt 模板 |
| **付费** | t_consumable_iap_record | 消耗品内购记录（能量） |
| | t_subscription_iap_record | 订阅内购记录（月度/年度） |
| | t_energy_trade_record | 能量交易流水 |
| **反馈** | t_feedback_info | 用户反馈 |
| **统计** | t_statistic_info | 收藏数等统计 |

### 关键字段速查

**t_user_info** — 用户
- `guid` (PK), `nick_name`, `user_new_id`

**t_account** — 账户
- `guid` (PK), `user_new_id`, `energy_num`, `resident_num`, `subscribe_status`, `subscribe_begin_time`, `subscribe_end_time`

**t_room_resident** — AI 玩偶
- `guid` (PK), `user_new_id`, `name`, `voice_type`, `personality_type`, `resident_persona` (人设), `resident_interest` (兴趣)

**t_chat_message** — 聊天消息
- `guid` (PK), `session_id`, `user_id`, `resident_id`, `content`, `sender_type` (1=AI/2=用户), `message_type` (1=文字/2=语音)

**t_chat_session** — 会话
- `guid` (PK), `user_id`, `resident_id`, `message_count`

**t_subscription_iap_record** — 订阅记录
- `guid` (PK), `user_new_id`, `subscription_type` (1月度/2年度), `subscription_status`, `product_id`, `price`, `currency`

**t_consumable_iap_record** — 消耗品内购
- `energy_count`, `price`

**t_energy_trade_record** — 能量流水
- `user_new_id`, `energy_num`, `trade_type`

---

## 五、sparklab_homework_monito（作业监督）— 11 张表

### 业务模块划分

| 模块 | 表名 | 说明 |
|------|------|------|
| **用户** | t_homework_user_info | 用户基础信息（年级、连续学习天数、累计任务数） |
| **登录** | t_user_login_record | 用户每日登录记录 |
| **任务** | t_homework_task_item | 作业/休息/阅读任务项（含学科分类、时长、状态） |
| **任务图片** | t_homework_task_item_image | 任务过程截图和作业成果图片 |
| **专注度** | t_homework_focus_score | 每次任务的专注度评分（0-100） |
| **走神** | t_homework_distraction | 走神瞬间截图 + 语音提示文案 |
| **学习报告** | t_study_report | AI 生成的学习报告（JSON） |
| **AI 解析** | t_ai_parse_record | 作业清单 AI 解析记录（图片/文本 → 结构化结果） |
| **上下文缓存** | t_ai_context_cache | AI 模型上下文缓存（场景码+模型ID+TTL） |
| **Prompt** | t_prompt | AI Prompt 配置（场景码、模型、上下线状态） |
| **虚拟人** | t_homework_virtual_person_config | 虚拟人打招呼/引导配置（分时段） |

### 关键字段速查

**t_homework_user_info** — 用户
- `guid` (PK), `user_new_id`, `nickname`, `avatar_url`, `grade` (年级), `status` (0正常/1注销), `continuous_study_days`, `total_completed_tasks`, `reminder_enabled` (0关/1开), `reminder_time` (如"18:00"), `total_experience` (经验值)

**t_homework_task_item** — 任务项
- `guid` (PK), `user_id`, `item_type` (1作业/2休息/3阅读), `item_name`, `duration` (分钟), `subject_type` (1语文/2数学/3英语/4其他), `subject_type_name`, `task_category` (作业/休息), `item_status` (0未开始/1进行中/2已完成/3已跳过), `completed_duration`, `batch_no`, `task_date`, `task_type` (1AI作业/2快速开始), `start_time`, `end_time`

**t_homework_focus_score** — 专注度
- `guid` (PK), `user_id`, `task_item_id`, `focus_score` (0-100), `record_time`, `image_url` (截图)

**t_homework_distraction** — 走神记录
- `guid` (PK), `user_id`, `task_item_id`, `image_url` (截图), `voice_prompt_text` (语音提示文案), `record_time`

**t_study_report** — 学习报告
- `guid` (PK), `user_id`, `task_item_id`, `batch_no`, `report_status` (0生成中/1已完成/2失败), `report_json` (加密)

**t_ai_parse_record** — AI 解析
- `guid` (PK), `user_id`, `batch_no`, `input_type` (text/image), `source_image_url`, `source_text`, `ai_parse_result` (JSON)

**t_prompt** — Prompt 配置
- `guid` (PK), `prompt_name`, `display_name`, `scene_code`, `model_id`, `priority`, `prompt_context` (加密), `use_status` (0下线/1上线)

**t_homework_virtual_person_config** — 虚拟人
- `guid` (PK), `config_type` (greeting/task_empty/task_guide), `time_range` (morning/afternoon/evening), `person_name`, `person_image_url`, `person_video_url`, `main_text`, `sub_text`, `bubble_text`, `is_enabled` (0禁用/1启用)

**t_user_login_record** — 登录记录
- `guid` (PK), `user_id`, `login_date`

**t_homework_task_item_image** — 任务图片
- `guid` (PK), `task_item_id`, `image_url`, `image_type` (1监督过程/2作业成果), `sort_order`

**t_ai_context_cache** — 上下文缓存
- `guid` (PK), `scene_code`, `model_id`, `prompt_fingerprint` (加密), `cache_id`, `ttl_seconds`, `cache_status` (1有效/0失效)

---

## 六、通用表结构规范

所有表遵循以下公约：

| 字段 | 类型 | 说明 |
|------|------|------|
| `guid` 或 `id` | bigint unsigned | 主键（雪花算法） |
| `create_time` | timestamp | 创建时间 |
| `update_time` | timestamp | 更新时间 |
| `is_delete` | smallint | 软删除标记（0=正常, 1=删除） |

**数据密级标注：** 字段 COLUMN_COMMENT 中包含 L1-L4 / S2 密级标记，L3+ 和 S2+ 为敏感数据，输出时需脱敏。

---

## 七、如何查看任意表的完整字段

```sql
SELECT COLUMN_NAME, COLUMN_TYPE, COLUMN_COMMENT
FROM information_schema.COLUMNS
WHERE TABLE_SCHEMA = '库名' AND TABLE_NAME = '表名'
ORDER BY ORDINAL_POSITION
```
